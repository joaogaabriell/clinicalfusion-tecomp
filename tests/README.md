# tests — Testes automatizados

Testes da aplicação (loaders, integração das modalidades, etc.).

Sugestão: `pytest` a partir da raiz do projeto:

```bash
pytest
```

> Placeholder. Os testes serão adicionados junto com a implementação.
